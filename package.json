{
  "name": "dprint-exec-reexport",
  "version": "0.0.8",
  "bin": "./dist/dprint-exec-reexport.mjs",
  "scripts": {
    "build": "tsdown",
    "prepublish": "bun run build"
  },
  "devDependencies": {
    "tsdown": "^0.15.7",
    "get-monorepo-root": "^1.2.1",
    "get-stdin": "^9.0.0"
  }
}
